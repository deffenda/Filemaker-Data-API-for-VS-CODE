node_modules
**/node_modules
dist
**/dist
.vscode-test
*.vsix
.DS_Store
.fmweb
.next
.npm-cache
.turbo
build
coverage

# Risks are private — do not publish to GitHub

# Pipeline state files must not be gitignored — they are required by the AI validator
